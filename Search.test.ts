// import { findPlayer } from './Search'

describe('player search', () => {
  // jest.setTimeout(10_000)
  // it("Returns Carmelo Anthony's page", async () => {
  //   const playerPage = await findPlayer('Anthony, Carmelo')
  //   const name = 'Carmelo Anthony'
  //   const includesName = playerPage?.body?.textContent?.includes(name) ?? false
  //   expect(includesName).toBe(true)
  // })

  it('returns true', () => {
    expect(true).toBeTruthy()
  })
})
